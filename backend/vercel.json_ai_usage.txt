# vercel.json GenAI Usage
--- Why it is a separate txt file? ---
Because vercel.json does not accept any additional fields that aims for comments. 
So, I document the ai usage here in a separate txt file

[GenAI Usage]: ChatGPT prompt: can you check why it is wrong (attached a screenshot of vercel deployment page with current build command, 
start command and root directory)?  want to deploy my backend: https://github.com/JJerryJi/130project

[GenAI Usage]: Responses are just content of vercel.json file 

[GenAI Usage] Reflection:
I utilize multi-modality of coding agent by providing details of my deployment issue along with my backend repo. 
It checks my Vercel deployment configuration and structure of the code before giving me the correct vercel.json file, a solution that 
shall be accepted.

[GenAI Usage] Codex Prompt:
Right now, the cron job defined in paper ingestion only run for one domain: CS.AI. I want to expand its scope to multi domain: physics, cs.db, cs.cv, math, life science etc. each with limit 500?
too much. i'm afraid it will throttle. maybe several cron job each with a domain run at different time -- each with an hour difference?

[GenAI Usage] Response begins:
Generated code lives in backend/vercel.json under the "crons" array. It defines separate paper-ingestion cron paths for cs-ai, cs-db, cs-cv, cs-lg, cs-cl, math, physics, and life-science. Each scheduled job runs one hour apart and the recommendation cron runs after the ingestion window.
[GenAI Usage] Response ends

[GenAI Usage] Reflection:
1. Cron job does setup by domain correctly. 
2. I chose the design decision to intentionally run each domain with 1 hour difference between different domains, because I found that arxiv API sometimes throttle due to too much requests. Therefore, it's best to wait for a long time. I intentionally made this waiting decision and Codex implemented it correctly
3. I inspected the domain names as well. I further verified and ensured they are all valid, key domain names defined in arxiv: https://arxiv.org/category_taxonomy.
4. In all, the code looks good and shall be accepted.
